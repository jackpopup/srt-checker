# Design: SRT Checker - 상세 설계서

## 1. 시스템 아키텍처

```
┌─────────────────────────────────────────────────────┐
│                    Browser (Client)                   │
│                                                       │
│  ┌──────────┐  ┌──────────────┐  ┌────────────────┐ │
│  │ 파일업로드 │→│ SRT Parser   │→│ 검사 요청 전송  │ │
│  │ (D&D/선택)│  │ (클라이언트)  │  │ (텍스트만)     │ │
│  └──────────┘  └──────────────┘  └───────┬────────┘ │
│                                           │          │
│  ┌──────────────────────────────────────┐ │          │
│  │ 결과 UI                              │ │          │
│  │ ┌─────────────┐ ┌─────────────────┐ │ │          │
│  │ │ DiffViewer  │ │ CheckboxList    │ │ │          │
│  │ │ (원본/수정) │ │ (선택적 적용)   │ │ │          │
│  │ └─────────────┘ └─────────────────┘ │ │          │
│  └──────────────────────────────────────┘ │          │
│                                           │          │
│  ┌────────────────┐  ┌────────────────┐  │          │
│  │ SRT 재조립     │  │ 파일 다운로드   │  │          │
│  │ (클라이언트)    │  │ (.srt)         │  │          │
│  └────────────────┘  └────────────────┘  │          │
└───────────────────────────────────────────┼──────────┘
                                            │
                    ┌───────────────────────┐│
                    │   Vercel Edge / API    ││
┌───────────────────┴───────────────────────┴┐
│              Next.js API Routes              │
│                                              │
│  POST /api/check-grammar                     │
│  ┌──────────────────────────────────────┐   │
│  │ 1. bkend에서 용어 사전 조회          │   │
│  │ 2. 프롬프트 조립 (텍스트+용어+규칙)  │   │
│  │ 3. Claude API 호출                   │   │
│  │ 4. 응답 파싱 및 검증                 │   │
│  │ 5. 수정 제안 목록 반환               │   │
│  └──────────────────────────────────────┘   │
│                                              │
│  GET/POST/PUT/DELETE /api/terms              │
│  POST /api/confluence/sync                   │
└──────────┬──────────────────┬────────────────┘
           │                  │
     ┌─────┴─────┐    ┌──────┴──────┐
     │  bkend.ai │    │ Claude API  │
     │ (용어 DB) │    │ (맞춤법)    │
     └───────────┘    └─────────────┘
```

## 2. API 설계

### 2.1 POST /api/check-grammar

맞춤법 검사의 핵심 엔드포인트.

**Request:**
```typescript
interface CheckGrammarRequest {
  // SRT에서 추출한 자막 텍스트 목록
  subtitles: {
    index: number;       // SRT 시퀀스 번호 (1-based)
    text: string;        // 대사 텍스트 (타임코드 제외)
  }[];
}
```

**Response:**
```typescript
interface CheckGrammarResponse {
  corrections: {
    index: number;        // SRT 시퀀스 번호
    original: string;     // 원본 텍스트
    corrected: string;    // 수정된 텍스트
    reason: string;       // 수정 이유 (한국어)
    type: "spelling" | "spacing" | "typo" | "terminology" | "grammar";
  }[];
  stats: {
    total_subtitles: number;    // 전체 자막 수
    checked_subtitles: number;  // 검사한 자막 수
    corrections_found: number;  // 수정 제안 수
    tokens_used: number;        // 사용된 토큰 수
  };
}
```

**처리 로직:**
1. 요청 검증 (subtitles 배열 비어있지 않은지, 텍스트 최대 길이 등)
2. bkend에서 용어 사전 조회 (캐시 활용)
3. 자막을 배치로 분할 (50개 단위) — 토큰 제한 대응
4. 각 배치에 대해 Claude API 호출
5. 응답 JSON 파싱 및 검증
6. 실패 시 최대 2회 재시도
7. 결과 합산하여 반환

**배치 분할 전략:**
- Claude API의 토큰 제한 고려 (입력+출력 합산)
- 자막 50개 단위로 분할 (평균 자막 30자 × 50 = 1,500자 + 프롬프트)
- 배치 간 순차 호출 (병렬 시 rate limit 위험)

### 2.2 GET /api/terms

용어 사전 목록 조회.

**Query Parameters:**
```
?category=product         # 카테고리 필터 (optional)
&search=비킷              # 검색어 (optional, term/aliases/description 대상)
&page=1&limit=50          # 페이지네이션
```

**Response:**
```typescript
interface TermsResponse {
  terms: {
    id: string;           // bkend document ID
    term: string;         // 정식 표기
    aliases: string[];    // 대체 표기 목록
    category: string;     // 카테고리
    description: string;  // 설명
  }[];
  total: number;
  page: number;
  limit: number;
}
```

### 2.3 POST /api/terms

용어 추가.

**Request:**
```typescript
interface CreateTermRequest {
  term: string;
  aliases: string[];
  category: string;
  description: string;
}
```

### 2.4 PUT /api/terms/[id]

용어 수정.

### 2.5 DELETE /api/terms/[id]

용어 삭제.

### 2.6 POST /api/confluence/sync

Confluence에서 용어를 수동으로 가져오기.

**Request:**
```typescript
interface ConfluenceSyncRequest {
  space_key?: string;    // 특정 스페이스 (optional, 기본: 전체)
}
```

**Response:**
```typescript
interface ConfluenceSyncResponse {
  synced_count: number;     // 가져온 용어 수
  new_terms: string[];      // 신규 추가된 용어명 목록
  updated_terms: string[];  // 갱신된 용어명 목록
  errors: string[];         // 에러 메시지 (있다면)
}
```

**처리 로직:**
1. 환경변수에서 Confluence 인증 정보 확인
2. Confluence CQL 검색: 제품명, 기술 용어가 포함된 페이지 조회
3. 페이지 본문에서 용어 후보 추출 (정규식 + 패턴 매칭)
4. 기존 용어 사전과 비교 → 신규/갱신 분류
5. bkend에 저장

## 3. 데이터 모델 (bkend)

### 3.1 Collection: `terms`

```json
{
  "_id": "auto",
  "term": "bkit",
  "aliases": ["비킷", "비kit", "b킷", "bKit", "비켓", "비킷트", "비키트"],
  "category": "product",
  "description": "Bkamp Vibecoding Kit. 클로드 코드 기반 AI 네이티브 개발 프레임워크.",
  "source": "initial",
  "created_at": "2026-02-04T00:00:00Z",
  "updated_at": "2026-02-04T00:00:00Z"
}
```

**인덱스:**
- `term` (unique)
- `category`
- `aliases` (array index, 검색용)

### 3.2 Collection: `sync_logs`

```json
{
  "_id": "auto",
  "synced_at": "2026-02-04T12:00:00Z",
  "source": "confluence",
  "space_key": "POPUPSTUDI",
  "terms_synced": 25,
  "new_terms": ["용어1", "용어2"],
  "updated_terms": ["용어3"],
  "status": "success"
}
```

## 4. 핵심 모듈 설계

### 4.1 SRT Parser (`lib/srt-parser.ts`)

```typescript
// --- 타입 ---
interface SrtEntry {
  index: number;           // 시퀀스 번호
  startTime: string;       // "00:01:23,456"
  endTime: string;         // "00:01:25,789"
  text: string;            // 대사 텍스트 (멀티라인 가능)
}

interface SrtFile {
  entries: SrtEntry[];
  raw: string;             // 원본 전체 텍스트 (복원용)
}

// --- 함수 ---

/** SRT 문자열 → 구조화된 객체 배열 */
function parseSrt(content: string): SrtFile

/** 수정사항 적용 후 SRT 문자열로 재조립 */
function buildSrt(entries: SrtEntry[]): string

/** SRT 엔트리에 수정사항 적용 (체크된 것만) */
function applyCorrections(
  entries: SrtEntry[],
  corrections: Correction[],
  selectedIndices: Set<number>
): SrtEntry[]
```

**파싱 규칙:**
- 빈 줄로 엔트리 구분
- 첫 줄: 시퀀스 번호 (정수)
- 둘째 줄: 타임코드 (`HH:MM:SS,mmm --> HH:MM:SS,mmm`)
- 나머지: 대사 텍스트 (여러 줄 가능)
- BOM 처리 (UTF-8 BOM 제거)
- CR/LF 정규화

### 4.2 Claude API Client (`lib/claude.ts`)

```typescript
interface ClaudeCheckOptions {
  subtitles: { index: number; text: string }[];
  terminology: TermEntry[];
  batchSize?: number;      // 기본 50
}

interface ClaudeCheckResult {
  corrections: Correction[];
  tokensUsed: number;
}

/** 맞춤법 검사 실행 */
async function checkGrammar(options: ClaudeCheckOptions): Promise<ClaudeCheckResult>

/** 프롬프트 조립 */
function buildPrompt(
  subtitles: { index: number; text: string }[],
  terminology: TermEntry[]
): string

/** 배치 분할 */
function splitIntoBatches<T>(items: T[], size: number): T[][]
```

### 4.3 Terminology Service (`lib/terminology.ts`)

```typescript
/** bkend에서 용어 사전 전체 조회 (캐시 포함) */
async function getAllTerms(): Promise<TermEntry[]>

/** 용어 검색 */
async function searchTerms(query: string, category?: string): Promise<TermEntry[]>

/** 용어 CRUD */
async function createTerm(data: CreateTermRequest): Promise<TermEntry>
async function updateTerm(id: string, data: Partial<TermEntry>): Promise<TermEntry>
async function deleteTerm(id: string): Promise<void>

/** 초기 데이터 시딩 (terminology-dictionary.json → bkend) */
async function seedTerms(): Promise<number>

/** Claude 프롬프트용 요약 사전 생성 */
function buildTermDictForPrompt(terms: TermEntry[]): string
```

**프롬프트용 요약 사전 형식:**
- 토큰 절약을 위해 전체 170개를 압축
- category별 그룹핑, aliases만 포함
- 예: `"bkit(비킷,비kit,b킷), bkend(비켄드,비캔드), ..."`

### 4.4 bkend Client (`lib/bkend.ts`)

```typescript
/** bkend REST API 클라이언트 */
class BkendClient {
  constructor(baseUrl: string, apiKey: string)

  // terms collection
  async getTerms(filter?: object): Promise<TermEntry[]>
  async createTerm(data: object): Promise<TermEntry>
  async updateTerm(id: string, data: object): Promise<TermEntry>
  async deleteTerm(id: string): Promise<void>

  // sync_logs collection
  async createSyncLog(data: object): Promise<SyncLog>
  async getSyncLogs(): Promise<SyncLog[]>
}
```

## 5. Claude API 프롬프트 상세 설계

### 5.1 시스템 프롬프트

```
당신은 한국어 영상 자막 맞춤법 교정 전문가입니다.
POPUP STUDIO AI의 영상 자막을 검수합니다.
```

### 5.2 유저 프롬프트 템플릿

```
아래 영상 자막의 맞춤법, 띄어쓰기, 오타를 검사해주세요.

## 절대 규칙
1. 타임코드, 시퀀스 번호, SRT 형식은 절대 건드리지 마세요. 여기서는 텍스트만 다룹니다.
2. 오직 대사 내용의 맞춤법, 띄어쓰기, 오타만 자연스럽게 교정하세요.
3. 문맥을 해치지 않는 선에서 수정하세요.
4. 영상 자막이므로 다음 구어적 표현은 수정하지 마세요:
   - 축약형: ~거든요, ~잖아, ~인데, ~같은데, ~하는데
   - 종결어미 변형: ~해요, ~하죠, ~할게요, ~할까요
   - 감탄사: 아, 어, 음, 네, 아니, 뭐, 진짜, 진짜요?
   - 구어체 조사: ~는요, ~은요, ~도요
5. 다음 용어 사전의 단어와 유사한 표현은 반드시 사전의 정식 표기를 따르세요:
   {termDictionary}

## 자막 목록
{subtitleList}

## 출력 형식 (반드시 JSON만 출력)
수정이 필요한 자막만 JSON 배열로 반환하세요. 수정이 없으면 []를 반환하세요.
```json
[
  {
    "index": 자막번호,
    "original": "원본 텍스트",
    "corrected": "수정된 텍스트",
    "reason": "수정 이유",
    "type": "spelling|spacing|typo|terminology|grammar"
  }
]
```
```

### 5.3 프롬프트 최적화 전략

| 전략 | 설명 |
|------|------|
| **용어 사전 압축** | 170개 전체 대신 aliases만 포함한 요약본 사용. ~2,000 토큰 |
| **배치 처리** | 50개 자막 단위로 분할. 배치당 ~3,000 입력 토큰 예상 |
| **타임코드 제거** | 클라이언트에서 텍스트만 추출하여 전송. 불필요한 토큰 절약 |
| **JSON 모드** | Claude의 JSON 응답 모드 활용하여 파싱 안정성 확보 |
| **모델 선택** | claude-sonnet-4-20250514 (비용 대비 성능 최적) |

## 6. 컴포넌트 설계

### 6.1 페이지 구조

```
app/
├── page.tsx                    # 메인: 업로드 + 검사 + 결과 (SPA)
├── terms/
│   └── page.tsx                # 용어 사전 관리
└── layout.tsx                  # 공통 레이아웃 (헤더, 네비게이션)
```

> 메인 페이지를 SPA로 구현하여 업로드→검사→결과→다운로드를 페이지 이동 없이 처리

### 6.2 메인 페이지 상태 흐름

```
[IDLE] ──업로드──▶ [FILE_LOADED] ──검사시작──▶ [CHECKING]
                                                  │
                                          ◀──실패── [ERROR]
                                                  │
                                          ──성공──▶ [RESULT]
                                                  │
                                          ──다운로드▶ [DOWNLOADED]
                                                  │
                                          ──새파일──▶ [IDLE]
```

```typescript
type AppState = "idle" | "file_loaded" | "checking" | "result" | "error";

interface AppStore {
  state: AppState;
  srtFile: SrtFile | null;
  corrections: Correction[];
  selectedCorrections: Set<number>;  // 체크된 correction index
  error: string | null;
  stats: CheckStats | null;
}
```

### 6.3 컴포넌트 트리

> 프로젝트 규모에 맞게 핵심 컴포넌트만 별도 파일로 분리하고, 나머지는 페이지 내 인라인으로 구현.

```
Layout (layout.tsx)
├── Header (components/layout/Header.tsx)
│
├── MainPage (page.tsx)
│   ├── FileUploader (components/upload/FileUploader.tsx)
│   │   └── 드래그앤드롭 + 파일 검증 (인라인)
│   │
│   ├── 검사 진행 UI (인라인 스피너)          # state: checking
│   │
│   ├── ResultPanel (components/result/ResultPanel.tsx)
│   │   ├── 통계 바 (인라인)
│   │   ├── 전체선택/해제 (인라인)
│   │   ├── CorrectionItem (components/result/CorrectionItem.tsx)
│   │   │   ├── Checkbox + DiffView (components/result/DiffView.tsx)
│   │   │   └── 유형 뱃지 + 사유 (인라인)
│   │   └── 다운로드 버튼 (인라인)
│   │
│   └── 에러 표시 (인라인)                    # state: error
│
└── TermsPage (terms/page.tsx) — 단일 "use client" 컴포넌트
    ├── 검색 + 카테고리 필터 (인라인)
    ├── 용어 테이블 + 수정/삭제 버튼 (인라인)
    ├── 용어 추가 모달 (인라인)
    ├── 용어 수정 모달 (인라인)
    ├── 삭제 확인 다이얼로그 (인라인)
    └── Confluence 동기화 (인라인)
```

### 6.4 CorrectionItem 상세 설계

각 수정 제안 항목의 UI:

```
┌─────────────────────────────────────────────────────┐
│ ☑  #23                                   [맞춤법]   │
│                                                      │
│  원본: 비킷은 바이브 코딩을 위한 프레임 워크입니다    │
│  수정: bkit은 바이브 코딩을 위한 프레임워크입니다     │
│        ────                        ─────────         │
│        용어 보정                    띄어쓰기 수정      │
│                                                      │
│  사유: "비킷"→"bkit" (용어 사전), "프레임 워크"→      │
│        "프레임워크" (띄어쓰기)                         │
└─────────────────────────────────────────────────────┘
```

- 체크박스: 좌측 상단
- 시퀀스 번호: `#23`
- 수정 유형 뱃지: 우측 상단 (색상 구분)
  - `spelling` 파란색, `spacing` 초록색, `typo` 빨간색, `terminology` 보라색, `grammar` 주황색
- 원본/수정 텍스트: 변경 부분 하이라이트 (인라인 diff)
- 수정 사유: 하단에 텍스트로 표시

### 6.5 인라인 Diff 알고리즘

단어 단위 diff를 사용하여 변경 부분만 하이라이트:

```typescript
interface DiffSegment {
  type: "equal" | "removed" | "added";
  text: string;
}

/** 두 문자열의 단어 단위 diff 생성 */
function computeWordDiff(original: string, corrected: string): {
  originalSegments: DiffSegment[];
  correctedSegments: DiffSegment[];
}
```

- `removed`: 빨간 배경 + 취소선
- `added`: 초록 배경 + 굵은 글씨
- `equal`: 기본 스타일

## 7. 용어 사전 페이지 설계

### 7.1 레이아웃

```
┌─────────────────────────────────────────────────────┐
│  SRT Checker    [검사하기]  [용어 사전]              │
├─────────────────────────────────────────────────────┤
│                                                      │
│  🔍 [검색어 입력...]         [+ 용어 추가]           │
│                                                      │
│  카테고리: [전체▾] [회사] [제품] [기능] [바이브코딩]  │
│           [AI도구] [AI개념] [개발] [플랫폼] [기업]    │
│                                                      │
│  ┌──────────────────────────────────────────────┐   │
│  │ 용어           │ 별칭          │ 설명        │   │
│  ├──────────────────────────────────────────────┤   │
│  │ bkit           │ 비킷, 비kit,  │ Bkamp Vibe  │   │
│  │ [product]      │ b킷, bKit    │ coding Kit.  │   │
│  │                │              │ 클로드 코드   │   │
│  │                │              │ 기반 AI ...  │   │
│  │                │         [수정] [삭제]       │   │
│  ├──────────────────────────────────────────────┤   │
│  │ bkend          │ 비켄드, ...   │ AI 네이티브  │   │
│  │ [product]      │              │ 백엔드 ...   │   │
│  │ ...            │              │              │   │
│  └──────────────────────────────────────────────┘   │
│                                                      │
│  ─── Confluence 동기화 ───                           │
│  [🔄 Confluence에서 용어 가져오기]                    │
│  마지막 동기화: 2026-02-04 12:00                     │
│                                                      │
│  총 170개 용어 | 페이지 1/4                          │
└─────────────────────────────────────────────────────┘
```

## 8. 환경 변수

```env
# Claude API
ANTHROPIC_API_KEY=sk-ant-...

# bkend (enduser API 패턴 — API Key 불필요)
NEXT_PUBLIC_BKEND_API_URL=https://api-enduser.bkend.ai
NEXT_PUBLIC_BKEND_PROJECT_ID=...
NEXT_PUBLIC_BKEND_ENVIRONMENT=dev

# Confluence (선택)
CONFLUENCE_URL=https://popupstudio.atlassian.net/wiki
CONFLUENCE_EMAIL=jack@popupstudio.ai
CONFLUENCE_API_TOKEN=...
```

## 9. 에러 처리

| 상황 | 처리 |
|------|------|
| SRT 파일 형식 오류 | 클라이언트에서 파싱 실패 시 "올바른 SRT 파일이 아닙니다" 표시 |
| 빈 SRT 파일 | "자막이 없는 파일입니다" 표시 |
| 5MB 초과 | 업로드 차단 + "5MB 이하 파일만 지원합니다" |
| Claude API 타임아웃 | 30초 후 타임아웃, 재시도 안내 |
| Claude API JSON 파싱 실패 | 최대 2회 재시도, 실패 시 "검사 실패" 표시 |
| Claude API rate limit | "잠시 후 다시 시도해주세요" 표시 |
| bkend 연결 실패 | 내장 기본 용어 사전 사용 (fallback) |
| Confluence API 인증 실패 | "Confluence 연결 설정을 확인해주세요" 표시 |

## 10. 프로젝트 구조 (최종)

```
srt-checker/
├── docs/
│   ├── 01-plan/
│   │   ├── plan-srt-checker.md
│   │   └── terminology-dictionary.json    # 초기 시드 데이터
│   └── 02-design/
│       └── design-srt-checker.md
├── src/
│   ├── app/
│   │   ├── layout.tsx                     # 공통 레이아웃
│   │   ├── page.tsx                       # 메인 (업로드+검사+결과)
│   │   ├── globals.css                    # Tailwind CSS
│   │   ├── terms/
│   │   │   └── page.tsx                   # 용어 사전 관리 (CRUD+검색+필터)
│   │   └── api/
│   │       ├── check-grammar/
│   │       │   └── route.ts               # 맞춤법 검사 API
│   │       ├── terms/
│   │       │   ├── route.ts               # 용어 목록/생성 (GET, POST)
│   │       │   ├── [id]/
│   │       │   │   └── route.ts           # 용어 수정/삭제 (PUT, DELETE)
│   │       │   └── seed/
│   │       │       └── route.ts           # 초기 데이터 시딩
│   │       └── confluence/
│   │           └── route.ts               # Confluence 동기화 + bkend 저장
│   ├── lib/
│   │   ├── srt-parser.ts                  # SRT 파싱/조립 (표준 + Premiere Pro)
│   │   ├── claude.ts                      # Claude API 클라이언트 (30s 타임아웃)
│   │   ├── bkend.ts                       # bkend REST 클라이언트
│   │   ├── terminology.ts                 # 용어 사전 로직 + 캐시 + 폴백
│   │   └── diff.ts                        # 단어 단위 diff 알고리즘
│   ├── components/
│   │   ├── layout/
│   │   │   └── Header.tsx                 # 로고 + 네비게이션
│   │   ├── upload/
│   │   │   └── FileUploader.tsx           # 드래그앤드롭 + 파일 검증
│   │   └── result/
│   │       ├── ResultPanel.tsx            # 결과 컨테이너 (통계+목록+다운로드)
│   │       ├── CorrectionItem.tsx         # 개별 교정 항목
│   │       └── DiffView.tsx              # 원본/수정 diff 하이라이트
│   ├── data/
│   │   └── terminology-dictionary.json    # 내장 폴백 용어 사전 (170+)
│   ├── hooks/
│   │   └── useCheckGrammar.ts             # 검사 API 호출 훅
│   └── types/
│       ├── srt.ts                         # SRT 관련 타입 (표준 + Premiere)
│       ├── correction.ts                  # 교정 관련 타입
│       └── term.ts                        # 용어 + SyncLog 타입
├── public/
│   └── favicon.ico
├── .env.local                             # 환경 변수 (gitignore)
├── .env.example                           # 환경 변수 예시
├── package.json
├── next.config.ts
├── tailwind.config.ts
├── tsconfig.json
└── CLAUDE.md
```

## 11. 구현 순서

| 단계 | 내용 | 의존성 |
|------|------|--------|
| 1 | 프로젝트 초기화 (Next.js + Tailwind) | 없음 |
| 2 | 타입 정의 (`types/`) | 없음 |
| 3 | SRT Parser (`lib/srt-parser.ts`) | 타입 |
| 4 | bkend 클라이언트 (`lib/bkend.ts`) | 타입 |
| 5 | 용어 사전 로직 + 시딩 API | bkend |
| 6 | Claude API 클라이언트 + 프롬프트 | 용어 사전 |
| 7 | 맞춤법 검사 API Route | Claude + bkend |
| 8 | Diff 알고리즘 (`lib/diff.ts`) | 없음 |
| 9 | 공통 컴포넌트 (Badge, Button, Modal) | 없음 |
| 10 | 메인 페이지 UI (업로드→검사→결과→다운로드) | 모든 lib + API |
| 11 | 용어 사전 페이지 UI | 용어 API |
| 12 | Confluence 동기화 API + UI | bkend |
| 13 | 에러 처리 + 로딩 상태 | 전체 |
| 14 | Vercel 배포 설정 | 전체 |
